import React from 'react';
import PropTypes from 'prop-types';

Error.propTypes = {
    
};

function Error(props) {
    return (
        <div>
            
        </div>
    );
}

export default Error;